{
  "name": "nexus",
  "version": "0.4.0-beta.2",
  "private": true,
  "type": "module",
  "description": "Nexus - extensible local model router for Codex and Cursor",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "check": "node scripts-check.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "proper-lockfile": "4.1.2"
  }
}
